doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Document
    body 
        lu 
            b Lista de servidores 
            each server in user.guilds
                li=server.name
        lu 
            b Lista de servidores del bot 
            each server in array 
                li=server.name